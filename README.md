# React + TypeScript + Vite

Firebase is used for authentication (Email/Password) and as a backend via Realtime Database. Data is fetched using Firebase SDK and REST principles.

Filtering and sorting are handled on the client side using memoized derived state, while pagination remains controlled by Firebase cursor-based loading.